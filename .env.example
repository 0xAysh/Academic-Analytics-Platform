# Database Configuration
# PostgreSQL connection settings
# You can use either DB_* or PG* format (use one format consistently)

# Option 1: Custom format (used by db/pool.js)
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user
DB_PASS=your_db_password
DB_NAME=your_database_name

# Option 2: PostgreSQL standard format (used by config/database.js)
# PGHOST=localhost
# PGPORT=5432
# PGUSER=your_db_user
# PGPASSWORD=your_db_password
# PGDATABASE=your_database_name

# JWT Secret
# Generate a secure random string for JWT token signing
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_here

# Server Port (optional - defaults to 3001 in server.js)
# PORT=3001
